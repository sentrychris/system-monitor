export enum SystemMetric {
  cpu = "cpu",
  mem = "memory",
  disk = "disk",
}

export enum Cpu {
  temp = "temp",
  freq = "freq",
  usage = "usage",
}

export enum Usage {
  used = "used",
  free = "free",
  total = "total",
}
