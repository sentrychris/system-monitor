export enum SystemMetric {
  cpu = "cpu",
  mem = "mem",
  disk = "disk"
}

export enum Cpu {
  temp = "temp",
  freq = "freq",
  usage = "usage",
}

export enum Usage {
  used = "used",
  free = "free",
  total = "total",
}